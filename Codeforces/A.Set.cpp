#include <iostream>
#include <cmath>

using namespace std;

bool caso()
{

    int n, m;

    cin >> n;

    m = n - 1;
    bool s[n], r[n - 1];

    for (int i = 0; i < n; i++)
    {
        cin >> s[i];
    }

    for (int i = 0; i < n; i++)
    {
        cin >> s[i];
    }
}
int main()
{

    // Se repite una vez por cada caso de prueba de la tarea
    int casos;

    cin >> casos;

    for (int i = 0; i < casos; i++)
    {
        caso();
    }

    return 0;
}